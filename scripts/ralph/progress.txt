## Codebase Patterns
- 使用 FastAPI async def 定义所有路由
- 使用 aiosqlite 异步操作 SQLite，数据库文件在 data/agent_world.db
- CREATE TABLE 必须加 IF NOT EXISTS
- 所有 API 响应遵循统一 JSON 格式 {success, data, message, request_id}
- Pydantic v2 模型用于请求/响应验证
- 服务层负责业务逻辑，路由层只做参数校验和响应格式化
- 测试文件命名 test_*.py，放在 tests/ 目录
- 测试使用 AGENT_WORLD_DB_PATH 环境变量指定独立测试数据库
- API Key 格式: agent-world- + secrets.token_hex(24) = 48位随机字符
- 认证中间件从 agent-auth-api-key 或 Authorization: Bearer 提取 Key
- 不要删除任何现有文件，只添加和修改
- 不要运行 git checkout/reset 等破坏性命令
- conftest.py 使用临时数据库隔离测试，避免用户名冲突
- 数据库单例 _db 需要在测试间重置，否则会复用旧连接


## 2026-05-04 22:30 - US-001
- 验证了数据库初始化与健康检查功能：database.py 包含11个表的 CREATE TABLE IF NOT EXISTS，/health 返回 {status:ok, db:connected}，测试全部通过
- 更改的文件：prd.json（passes: true），.gitignore（新增），progress.txt（追加）
- **未来迭代的学习：**
  - codebase 已有完整的数据库建表和健康检查实现，US-001 的代码已就绪
  - 测试使用 AGENT_WORLD_DB_PATH 环境变量隔离测试数据库
  - 初始 git repo 需要手动设置 user.email/user.name
  - 排除 __pycache__、.pyc、data/*.db 到 .gitignore
---

## 2026-05-04 02:07 - US-002
- 验证了 Agent 注册接口功能：POST /api/agents/register 完整实现，包含 username 验证(2-50字符a-z0-9_-)、唯一性检查、挑战题生成(混淆数学题)、持久化到 agents 表
- 修复了 test_health.py 中 test_db_file_location 在 :memory: 模式下的失败（添加 skip）
- 更改的文件：prd.json（passes: true）、tests/test_health.py（修复测试兼容性）、progress.txt（追加）
- **未来迭代的学习：**
  - US-002 的代码（路由/服务/模型/测试）已在上一次迭代中完整实现，本次仅验证通过
  - test_health.py 的 test_db_file_location 需要 skip :memory: 模式
  - 已有 challenge.py 负责生成混淆数学题，auth.py 负责认证中间件，verify 路由也已存在
---

## 2026-05-04 22:50 - US-003
- 验证了挑战题验证与激活功能：POST /api/agents/verify 完整实现，包含验证码校验、5分钟过期、数字答案验证、api_key生成(agent-world-+48位)、错误次数扣减、5次失败删除
- 新增 tests/conftest.py（临时数据库隔离），tests/test_verify.py（新增过期/已激活/非数字答案测试）
- 更改的文件：tests/conftest.py（新增）、tests/test_verify.py（新增3个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - 测试间共享数据库单例会导致用户名冲突，conftest.py 通过临时数据库+每次测试重置解决
  - verify 路由已实现完整：验证码查找、过期检查、答案比较(浮点容差0.01)、激活/删除逻辑
  - 已激活账号的验证码会被清空（verification_code = ''），防止重用
---

## 2026-05-04 02:56 - US-004
- 实现了 API Key 认证中间件：auth.py 提供 get_current_agent 依赖，支持 agent-auth-api-key 和 Authorization: Bearer 两种方式；新增 /api/agents/me 端点验证中间件
- 更改的文件：src/services/auth.py（已存在，新增）、src/api/routes/agents.py（添加 Depends 导入和 /me 路由）、tests/test_auth.py（新增7个测试覆盖所有验收标准）
- **未来迭代的学习：**
  - get_current_agent 作为 FastAPI Depends 注入，返回 dict{agent_id, username, nickname}
  - 中间件中 agent-auth-api-key 优先于 Authorization: Bearer（两种同时存在时取前者）
  - 401/403 错误通过 HTTPException detail=dict 返回，包含 error/message/hint
  - 测试中通过 _create_active_agent helper 快速获取有效 API Key
---

## 2026-05-04 03:03 - US-005
- 实现了 Profile 查询与修改：GET /api/agents/profile/{username} 公开查询返回 username/nickname/avatar_url/bio/created_at，不存在返回 error_response(not_found)；PUT /api/agents/profile 需 API Key 认证，通过 UpdateProfileRequest Pydantic 模型限制 nickname<=100/bio<=500 超长 422，只允许修改 nickname/bio 不允许改 username/agent_id
- 更改的文件：src/models/schemas.py（新增 UpdateProfileRequest）、src/api/routes/agents.py（新增 get_profile/update_profile 路由）、tests/test_profile.py（新增 10 个测试）
- **未来迭代的学习：**
  - UpdateProfileRequest 使用 `str | None = Field(None, max_length=N)` 模式，只在非 None 时更新，支持部分更新
  - error_response 的 not_found 不返回 HTTP 404 状态码，而是返回 200 + success:false（与现有 pattern 一致）
  - PUT 路由复用 get_current_agent 依赖注入，无需额外认证逻辑
---

## 2026-05-04 23:30 - US-006
- 实现了头像上传与 AI 默认头像：POST /api/agents/avatar 支持 JPEG/PNG/WebP/GIF（415 拒绝不支持的类型），5MB 限制（413 超大文件），上传后更新 avatar_url；激活时 Pillow 自动生成抽象图案默认头像（基于 agent_id 的种子随机圆形/矩形叠加）
- 更改的文件：src/services/avatar.py（新增）、src/api/routes/agents.py（添加 upload_avatar 路由 + verify 路由增加默认头像生成）、src/main.py（添加 StaticFiles 挂载）、.gitignore（添加 data/avatars/）、tests/test_avatar.py（新增 7 个测试）、tests/test_profile.py（修复 avatar_url 断言）
- **未来迭代的学习：**
  - 头像文件存储在 data/avatars/ 目录，avatar_url 格式为 /data/avatars/{agent_id}.{ext}
  - Pillow 默认头像生成使用 agent_id UUID 作为随机种子，保证同一 agent 每次生成相同图案
  - StaticFiles 挂载需要在模块加载时确保目录存在，否则会抛异常
  - 新功能（默认头像）可能影响已有测试的断言，如 test_profile.py 的 avatar_url 空字符串断言需要更新
---

## 2026-05-04 04:19 - US-007
- 实现了联盟站点 Key 验证：POST /api/agents/verify-key 需 x-site-id + x-site-secret 站点凭证，验证成功返回 agent_id/username/nickname/is_active，凭证无效返回 401，API Key 无效返回 success=false
- 更改的文件：src/models/schemas.py（新增 VerifyKeyRequest）、src/api/routes/agents.py（新增 verify_key 路由）、tests/test_verify_key.py（新增 6 个测试）
- **未来迭代的学习：**
  - verify_site 已在 auth.py 中实现，作为 FastAPI Depends 使用，验证 x-site-id 和 x-site-secret headers
  - verify-site 使用 Header(...) 必填参数，缺失时自动返回 422（Pydantic 验证）
  - sites 表已在 database.py 中创建，测试需手动插入站点记录
  - verify-key 端点不限制 is_active，未激活 agent 也能查询到（返回 is_active=false）
---

## 2026-05-04 23:55 - US-010
- 实现了虾评-技能列表与详情：GET /api/skills 支持分页(page/limit默认20)、关键词搜索(search)、分类筛选(category)、排序(newest/downloads/rating)；GET /api/skills/{id} 返回完整技能详情含 author 信息；GET /api/categories 返回分类及技能数
- 更改的文件：src/api/routes/skills.py（新增）、src/main.py（注册 skills_router）、tests/test_skills.py（新增12个测试）
- **未来迭代的学习：**
  - skills 路由使用 APIRouter(prefix="/api") 而非 prefix="/api/skills"，因为还需要挂载 /api/categories
  - 列表查询通过 LEFT JOIN agents 获取 author username
  - 软删除通过 deleted_at IS NULL 过滤，列表和详情都需要过滤
  - 测试间 skills 表数据会累积，需要在测试文件中添加 autouse fixture 清空 skills 表
  - _insert_skill helper 直接操作数据库插入测试数据，避免依赖未实现的发布 API
---

## 2026-05-04 25:00 - US-011
- 实现了虾评-技能发布更新删除：POST /api/skills（需 API Key，name 必填，发布后 +10 虾米到 wallets），PUT /api/skills/{id}（仅作者可更新，非作者返回 forbidden），DELETE /api/skills/{id}（仅作者可软删除，标记 deleted_at）
- 更改的文件：src/models/schemas.py（新增 CreateSkillRequest、UpdateSkillRequest）、src/api/routes/skills.py（新增 POST/PUT/DELETE 路由）、tests/test_skill_crud.py（新增 11 个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - wallets 表使用 ON CONFLICT(agent_id) DO UPDATE 实现"有则加、无则创建"的虾米累加
  - 技能权限校验通过比较 author_id == agent["agent_id"] 实现，非作者统一返回 error_response("forbidden", ...)
  - 软删除后 GET 详情和列表都通过 deleted_at IS NULL / row["deleted_at"] 检查过滤
  - UpdateSkillRequest 使用 `str | None = Field(None)` 模式实现部分更新，只更新非 None 字段
---

## 2026-05-04 04:36 - US-012
- 实现了虾评-技能下载与收藏：GET /api/skills/{id}/download（试用版免费，正式版-2虾米，下载计数+1，余额不足报错），POST /api/skills/{id}/favorite（收藏，重复收藏报错），DELETE /api/skills/{id}/favorite（取消收藏），GET /api/me/favorites（分页收藏列表含技能详情）
- 新增 downloads 表到 database.py 用于记录下载历史
- 更改的文件：src/api/routes/skills.py（新增4个路由）、src/services/database.py（新增downloads表）、tests/test_download_favorites.py（新增16个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - 试用版/正式版通过 skills.version 字段区分，version=='draft' 为试用版（免费），否则为正式版（-2虾米）
  - 下载扣费前需检查 wallets 余额，不足返回 insufficient_balance 错误
  - 收藏使用 favorites 表的 UNIQUE(agent_id, skill_id) 约束防止重复
  - downloads 表记录下载历史，为 US-015 的"下载记录"做准备
  - 收藏列表 JOIN skills 时需过滤 deleted_at IS NULL，避免展示已删除技能
---

## 2026-05-04 23:30 - US-013
- 实现了虾评-技能评测系统：POST /api/skills/{id}/comments（需 API Key，rating 必填 1-5，可选维度 functionality/effectiveness/scarcity 各 1-5），虾米奖励（基础+1、完整3维度+3、含 model_info 额外+1），限流每小时3条每天10条，评测后更新技能 rating 和 rating_count，禁止评测自己的技能
- 更改的文件：src/models/schemas.py（新增 CreateReviewRequest）、src/api/routes/skills.py（新增 create_review 路由）、tests/test_reviews.py（新增12个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - reviews 表已在 database.py 中预建，包含 functionality/effectiveness/scarcity/model_info 字段
  - 评测奖励分层：仅 rating=+1，rating+3维度=+3，再+model_info=+4
  - 限流使用 SQLite datetime(?, '-1 hour')/datetime(?, '-1 day') 计算时间窗口
  - 技能总评分使用 AVG(rating) 重算并 round 到2位小数
  - 测试中 _clean_tables 需同时清空 agents 表，否则用户名冲突导致注册失败
---

## 2026-05-04 05:14 - US-014
- 实现了虾评-许愿墙：POST /api/wishes（需 API Key，content 必填，+2 虾米，最多 3 个待实现心愿），GET /api/wishes（无需认证，分页列表），POST /api/wishes/{id}/vote（投票，+1 虾米给发布者，每人每心愿 1 票）
- 新增 wish_votes 表到 database.py 用于追踪投票记录（UNIQUE(wish_id, agent_id) 防止重复投票）
- 更改的文件：src/api/routes/skills.py（新增3个路由）、src/services/database.py（新增wish_votes表）、src/models/schemas.py（新增CreateWishRequest）、tests/test_wishes.py（新增13个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - 许愿墙限制为每个 agent 最多 3 个 status='pending' 的心愿，已实现/已关闭的不计入
  - 投票使用独立 wish_votes 表追踪，与 favorites 模式一致（UNIQUE约束防重复）
  - 投票奖励直接给心愿发布者（非投票者），使用 ON CONFLICT(agent_id) DO UPDATE 累加
  - 测试中需要多个 agent 创建心愿时才能超过单 agent 3个限制来测试分页
---
