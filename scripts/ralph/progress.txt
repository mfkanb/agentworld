## Codebase Patterns
- 使用 FastAPI async def 定义所有路由
- 使用 aiosqlite 异步操作 SQLite，数据库文件在 data/agent_world.db
- CREATE TABLE 必须加 IF NOT EXISTS
- 所有 API 响应遵循统一 JSON 格式 {success, data, message, request_id}
- Pydantic v2 模型用于请求/响应验证
- 服务层负责业务逻辑，路由层只做参数校验和响应格式化
- 测试文件命名 test_*.py，放在 tests/ 目录
- 测试使用 AGENT_WORLD_DB_PATH 环境变量指定独立测试数据库
- API Key 格式: agent-world- + secrets.token_hex(24) = 48位随机字符
- 认证中间件从 agent-auth-api-key 或 Authorization: Bearer 提取 Key
- 不要删除任何现有文件，只添加和修改
- 不要运行 git checkout/reset 等破坏性命令
- conftest.py 使用临时数据库隔离测试，避免用户名冲突
- 数据库单例 _db 需要在测试间重置，否则会复用旧连接
- 测试必须使用 @pytest.mark.anyio 而非 @pytest.mark.asyncio（strict mode 兼容性）
- 种子数据（seed）使用"表为空时插入"模式，通过 SELECT COUNT(*) 检查


## 2026-05-04 22:30 - US-001
- 验证了数据库初始化与健康检查功能：database.py 包含11个表的 CREATE TABLE IF NOT EXISTS，/health 返回 {status:ok, db:connected}，测试全部通过
- 更改的文件：prd.json（passes: true），.gitignore（新增），progress.txt（追加）
- **未来迭代的学习：**
  - codebase 已有完整的数据库建表和健康检查实现，US-001 的代码已就绪
  - 测试使用 AGENT_WORLD_DB_PATH 环境变量隔离测试数据库
  - 初始 git repo 需要手动设置 user.email/user.name
  - 排除 __pycache__、.pyc、data/*.db 到 .gitignore
---

## 2026-05-04 02:07 - US-002
- 验证了 Agent 注册接口功能：POST /api/agents/register 完整实现，包含 username 验证(2-50字符a-z0-9_-)、唯一性检查、挑战题生成(混淆数学题)、持久化到 agents 表
- 修复了 test_health.py 中 test_db_file_location 在 :memory: 模式下的失败（添加 skip）
- 更改的文件：prd.json（passes: true）、tests/test_health.py（修复测试兼容性）、progress.txt（追加）
- **未来迭代的学习：**
  - US-002 的代码（路由/服务/模型/测试）已在上一次迭代中完整实现，本次仅验证通过
  - test_health.py 的 test_db_file_location 需要 skip :memory: 模式
  - 已有 challenge.py 负责生成混淆数学题，auth.py 负责认证中间件，verify 路由也已存在
---

## 2026-05-04 22:50 - US-003
- 验证了挑战题验证与激活功能：POST /api/agents/verify 完整实现，包含验证码校验、5分钟过期、数字答案验证、api_key生成(agent-world-+48位)、错误次数扣减、5次失败删除
- 新增 tests/conftest.py（临时数据库隔离），tests/test_verify.py（新增过期/已激活/非数字答案测试）
- 更改的文件：tests/conftest.py（新增）、tests/test_verify.py（新增3个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - 测试间共享数据库单例会导致用户名冲突，conftest.py 通过临时数据库+每次测试重置解决
  - verify 路由已实现完整：验证码查找、过期检查、答案比较(浮点容差0.01)、激活/删除逻辑
  - 已激活账号的验证码会被清空（verification_code = ''），防止重用
---

## 2026-05-04 02:56 - US-004
- 实现了 API Key 认证中间件：auth.py 提供 get_current_agent 依赖，支持 agent-auth-api-key 和 Authorization: Bearer 两种方式；新增 /api/agents/me 端点验证中间件
- 更改的文件：src/services/auth.py（已存在，新增）、src/api/routes/agents.py（添加 Depends 导入和 /me 路由）、tests/test_auth.py（新增7个测试覆盖所有验收标准）
- **未来迭代的学习：**
  - get_current_agent 作为 FastAPI Depends 注入，返回 dict{agent_id, username, nickname}
  - 中间件中 agent-auth-api-key 优先于 Authorization: Bearer（两种同时存在时取前者）
  - 401/403 错误通过 HTTPException detail=dict 返回，包含 error/message/hint
  - 测试中通过 _create_active_agent helper 快速获取有效 API Key
---

## 2026-05-04 03:03 - US-005
- 实现了 Profile 查询与修改：GET /api/agents/profile/{username} 公开查询返回 username/nickname/avatar_url/bio/created_at，不存在返回 error_response(not_found)；PUT /api/agents/profile 需 API Key 认证，通过 UpdateProfileRequest Pydantic 模型限制 nickname<=100/bio<=500 超长 422，只允许修改 nickname/bio 不允许改 username/agent_id
- 更改的文件：src/models/schemas.py（新增 UpdateProfileRequest）、src/api/routes/agents.py（新增 get_profile/update_profile 路由）、tests/test_profile.py（新增 10 个测试）
- **未来迭代的学习：**
  - UpdateProfileRequest 使用 `str | None = Field(None, max_length=N)` 模式，只在非 None 时更新，支持部分更新
  - error_response 的 not_found 不返回 HTTP 404 状态码，而是返回 200 + success:false（与现有 pattern 一致）
  - PUT 路由复用 get_current_agent 依赖注入，无需额外认证逻辑
---

## 2026-05-04 23:30 - US-006
- 实现了头像上传与 AI 默认头像：POST /api/agents/avatar 支持 JPEG/PNG/WebP/GIF（415 拒绝不支持的类型），5MB 限制（413 超大文件），上传后更新 avatar_url；激活时 Pillow 自动生成抽象图案默认头像（基于 agent_id 的种子随机圆形/矩形叠加）
- 更改的文件：src/services/avatar.py（新增）、src/api/routes/agents.py（添加 upload_avatar 路由 + verify 路由增加默认头像生成）、src/main.py（添加 StaticFiles 挂载）、.gitignore（添加 data/avatars/）、tests/test_avatar.py（新增 7 个测试）、tests/test_profile.py（修复 avatar_url 断言）
- **未来迭代的学习：**
  - 头像文件存储在 data/avatars/ 目录，avatar_url 格式为 /data/avatars/{agent_id}.{ext}
  - Pillow 默认头像生成使用 agent_id UUID 作为随机种子，保证同一 agent 每次生成相同图案
  - StaticFiles 挂载需要在模块加载时确保目录存在，否则会抛异常
  - 新功能（默认头像）可能影响已有测试的断言，如 test_profile.py 的 avatar_url 空字符串断言需要更新
---

## 2026-05-04 04:19 - US-007
- 实现了联盟站点 Key 验证：POST /api/agents/verify-key 需 x-site-id + x-site-secret 站点凭证，验证成功返回 agent_id/username/nickname/is_active，凭证无效返回 401，API Key 无效返回 success=false
- 更改的文件：src/models/schemas.py（新增 VerifyKeyRequest）、src/api/routes/agents.py（新增 verify_key 路由）、tests/test_verify_key.py（新增 6 个测试）
- **未来迭代的学习：**
  - verify_site 已在 auth.py 中实现，作为 FastAPI Depends 使用，验证 x-site-id 和 x-site-secret headers
  - verify-site 使用 Header(...) 必填参数，缺失时自动返回 422（Pydantic 验证）
  - sites 表已在 database.py 中创建，测试需手动插入站点记录
  - verify-key 端点不限制 is_active，未激活 agent 也能查询到（返回 is_active=false）
---

## 2026-05-04 23:55 - US-010
- 实现了虾评-技能列表与详情：GET /api/skills 支持分页(page/limit默认20)、关键词搜索(search)、分类筛选(category)、排序(newest/downloads/rating)；GET /api/skills/{id} 返回完整技能详情含 author 信息；GET /api/categories 返回分类及技能数
- 更改的文件：src/api/routes/skills.py（新增）、src/main.py（注册 skills_router）、tests/test_skills.py（新增12个测试）
- **未来迭代的学习：**
  - skills 路由使用 APIRouter(prefix="/api") 而非 prefix="/api/skills"，因为还需要挂载 /api/categories
  - 列表查询通过 LEFT JOIN agents 获取 author username
  - 软删除通过 deleted_at IS NULL 过滤，列表和详情都需要过滤
  - 测试间 skills 表数据会累积，需要在测试文件中添加 autouse fixture 清空 skills 表
  - _insert_skill helper 直接操作数据库插入测试数据，避免依赖未实现的发布 API
---

## 2026-05-04 25:00 - US-011
- 实现了虾评-技能发布更新删除：POST /api/skills（需 API Key，name 必填，发布后 +10 虾米到 wallets），PUT /api/skills/{id}（仅作者可更新，非作者返回 forbidden），DELETE /api/skills/{id}（仅作者可软删除，标记 deleted_at）
- 更改的文件：src/models/schemas.py（新增 CreateSkillRequest、UpdateSkillRequest）、src/api/routes/skills.py（新增 POST/PUT/DELETE 路由）、tests/test_skill_crud.py（新增 11 个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - wallets 表使用 ON CONFLICT(agent_id) DO UPDATE 实现"有则加、无则创建"的虾米累加
  - 技能权限校验通过比较 author_id == agent["agent_id"] 实现，非作者统一返回 error_response("forbidden", ...)
  - 软删除后 GET 详情和列表都通过 deleted_at IS NULL / row["deleted_at"] 检查过滤
  - UpdateSkillRequest 使用 `str | None = Field(None)` 模式实现部分更新，只更新非 None 字段
---

## 2026-05-04 04:36 - US-012
- 实现了虾评-技能下载与收藏：GET /api/skills/{id}/download（试用版免费，正式版-2虾米，下载计数+1，余额不足报错），POST /api/skills/{id}/favorite（收藏，重复收藏报错），DELETE /api/skills/{id}/favorite（取消收藏），GET /api/me/favorites（分页收藏列表含技能详情）
- 新增 downloads 表到 database.py 用于记录下载历史
- 更改的文件：src/api/routes/skills.py（新增4个路由）、src/services/database.py（新增downloads表）、tests/test_download_favorites.py（新增16个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - 试用版/正式版通过 skills.version 字段区分，version=='draft' 为试用版（免费），否则为正式版（-2虾米）
  - 下载扣费前需检查 wallets 余额，不足返回 insufficient_balance 错误
  - 收藏使用 favorites 表的 UNIQUE(agent_id, skill_id) 约束防止重复
  - downloads 表记录下载历史，为 US-015 的"下载记录"做准备
  - 收藏列表 JOIN skills 时需过滤 deleted_at IS NULL，避免展示已删除技能
---

## 2026-05-04 23:30 - US-013
- 实现了虾评-技能评测系统：POST /api/skills/{id}/comments（需 API Key，rating 必填 1-5，可选维度 functionality/effectiveness/scarcity 各 1-5），虾米奖励（基础+1、完整3维度+3、含 model_info 额外+1），限流每小时3条每天10条，评测后更新技能 rating 和 rating_count，禁止评测自己的技能
- 更改的文件：src/models/schemas.py（新增 CreateReviewRequest）、src/api/routes/skills.py（新增 create_review 路由）、tests/test_reviews.py（新增12个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - reviews 表已在 database.py 中预建，包含 functionality/effectiveness/scarcity/model_info 字段
  - 评测奖励分层：仅 rating=+1，rating+3维度=+3，再+model_info=+4
  - 限流使用 SQLite datetime(?, '-1 hour')/datetime(?, '-1 day') 计算时间窗口
  - 技能总评分使用 AVG(rating) 重算并 round 到2位小数
  - 测试中 _clean_tables 需同时清空 agents 表，否则用户名冲突导致注册失败
---

## 2026-05-04 05:14 - US-014
- 实现了虾评-许愿墙：POST /api/wishes（需 API Key，content 必填，+2 虾米，最多 3 个待实现心愿），GET /api/wishes（无需认证，分页列表），POST /api/wishes/{id}/vote（投票，+1 虾米给发布者，每人每心愿 1 票）
- 新增 wish_votes 表到 database.py 用于追踪投票记录（UNIQUE(wish_id, agent_id) 防止重复投票）
- 更改的文件：src/api/routes/skills.py（新增3个路由）、src/services/database.py（新增wish_votes表）、src/models/schemas.py（新增CreateWishRequest）、tests/test_wishes.py（新增13个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - 许愿墙限制为每个 agent 最多 3 个 status='pending' 的心愿，已实现/已关闭的不计入
  - 投票使用独立 wish_votes 表追踪，与 favorites 模式一致（UNIQUE约束防重复）
  - 投票奖励直接给心愿发布者（非投票者），使用 ON CONFLICT(agent_id) DO UPDATE 累加
  - 测试中需要多个 agent 创建心愿时才能超过单 agent 3个限制来测试分页
---

## 2026-05-04 18:30 - US-015
- 实现了虾评-个人中心与排行榜：GET /api/auth/me（返回虾米余额和等级，等级按虾米自动计算A1/A2-1/A2-2/A3-1/A3-2/A4-1），GET /api/me/skills（我的技能列表，分页，排除已删除），GET /api/me/downloads（我的下载记录，分页，含技能详情），GET /api/rankings（虾米排行榜，无需认证，按余额降序）
- 更改的文件：src/api/routes/skills.py（新增4个路由+等级计算函数）、tests/test_profile_rankings.py（新增18个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - 等级阈值：A1(0) → A2-1(100) → A2-2(500) → A3-1(1000) → A3-2(3000) → A4-1(10000)
  - /api/auth/me 和 /api/me/* 路由放在 skills.py 中（prefix="/api"），与 /me/favorites 保持一致
  - 排行榜只显示 is_active=1 的 agent，通过 JOIN agents 过滤
  - 无钱包记录时余额默认为 0，等级为 A1
---

## 2026-05-04 05:41 - US-020
- 实现了酒馆-酒水系统：drinks 表预置 15 款创意酒水（含 name/code/description/tags/taste_tags/effect_tags），POST /drink/random 随机点酒创建 session，POST /drink 按 code 指定点酒，POST /sessions/{id}/consume 消耗返回效果（relaxation_index + mood_tags），GET /drinks 酒谱列表（无需认证），每日 10 杯上限
- 更改的文件：src/services/drink_seeds.py（新增 15 款预设酒水+seed 函数）、src/api/routes/bar.py（新增 4 个路由）、src/models/schemas.py（新增 OrderDrinkRequest）、src/main.py（注册 bar_router + 启动时 seed_drinks）、tests/test_bar.py（新增 13 个测试）
- **未来迭代的学习：**
  - 测试必须使用 `@pytest.mark.anyio` 而不是 `@pytest.mark.asyncio`，否则 conftest.py 的 _reset_db autouse fixture 会报 PytestRemovedIn9Warning
  - 酒水 seed 使用"表为空时插入"模式（SELECT COUNT(*) 检查），避免重复插入
  - drink_sessions 表关联 agent_id 和 drink_id，consumed 字段标记是否已消耗（0/1）
  - 消耗效果使用 random 模块生成，mood_tags 从预设列表随机采样 2-4 个
  - 每日限流通过 datetime(?, '-1 day') 计算 24 小时窗口，random 和 order 共享同一个检查函数
---

## 2026-05-04 05:48 - US-021
- 实现了酒馆-留言簿：POST /guestbook/entries（需 API Key，content 必填，可选关联 drink_session_id，限流 30 秒 1 条，敏感信息自动过滤 API Key/邮箱/手机号），GET /guestbook（分页按时间倒序，无需认证），POST /guestbook/entries/{id}/like（点赞，UNIQUE 防重复），DELETE /guestbook/entries/{id}（只能删自己的）
- 新增 guestbook_likes 表到 database.py 用于追踪点赞记录（UNIQUE(entry_id, agent_id) 防重复点赞）
- 更改的文件：src/api/routes/bar.py（新增4个路由+敏感过滤函数）、src/services/database.py（新增guestbook_likes表）、src/models/schemas.py（新增CreateGuestbookEntryRequest）、tests/test_guestbook.py（新增16个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - 留言簿路由放在 bar.py 中，与酒水系统共用同一个 router（tags=["bar"]）
  - 敏感信息过滤使用正则替换：API Key（agent-world-xxx48位hex）、邮箱、手机号（1[3-9]\d{9}）
  - guestbook_likes 表结构与 wish_votes/favorites 一致，使用 UNIQUE 约束防重复
  - 删除留言时需同时删除关联的 guestbook_likes 记录
  - 限流使用 datetime(?, '-30 seconds') 时间窗口计算
---

## 2026-05-04 05:53 - US-022
- 实现了酒馆-涂鸦墙：POST /selfies（需 API Key，Pillow 自动生成抽象涂鸦图案，含圆形/矩形/线条叠加），GET /selfies（无需认证，分页按时间倒序），POST /selfies/{id}/like（点赞，UNIQUE 防重复），DELETE /selfies/{id}（只能删自己的，同时删除图片文件和关联点赞）
- 新增 selfie_likes 表到 database.py 用于追踪点赞记录（UNIQUE(selfie_id, agent_id) 防重复点赞）
- 更改的文件：src/api/routes/bar.py（新增4个路由+_generate_doodle函数）、src/services/database.py（新增selfie_likes表）、src/main.py（添加selfies目录创建）、tests/test_selfies.py（新增13个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - 涂鸦生成使用 hashlib.md5(agent_id + timestamp) 作为种子，确保每次生成不同图案
  - 涂鸦图片保存到 data/selfies/ 目录，文件名格式 {agent_id[:8]}_{random_hex}.png
  - 删除涂鸦时需同时删除 selfie_likes 记录和物理图片文件
  - selfie_likes 表结构与 guestbook_likes/wish_votes/favorites 一致，使用 UNIQUE 约束防重复
  - Pillow 涂鸦图案比头像多了一种 "line" 形状类型，背景偏深色以突出涂鸦效果
---

## 2026-05-04 05:59 - US-023
- 验证了全局限流中间件功能：RateLimitMiddleware 已在 main.py 注册，GET 60次/分钟 POST 30次/分钟，429 状态码 + Retry-After Header，API Key 优先/IP 兜底限流，7个测试全部通过
- 中间件实现使用滑动窗口（timestamp 列表 + 60秒窗口清理），非令牌桶
- 更改的文件：prd.json（passes: true）、progress.txt（追加）
- **未来迭代的学习：**
  - RateLimitMiddleware 继承 BaseHTTPMiddleware，通过 app.add_middleware() 注册
  - 限流 key 优先从 agent-auth-api-key / Authorization: Bearer 提取 API Key，无则用 x-forwarded-for / client.host IP
  - 中间件只对 /api/ 路径和 /、/health 限流，其他路径（如 /data/* 静态文件）不限流
  - 测试中通过 autouse fixture 重置 RateLimitMiddleware._requests = {} 清空限流状态
  - 测试中大量请求可能耗时，179 个测试总计约 17 秒
---

## 2026-05-04 23:10 - US-023 (验证)
- 验证了全局限流中间件：重构 rate_limit.py 为独立的 RateLimiter 类 + 全局 limiter 单例，conftest.py 在每个测试间调用 limiter.reset() 避免跨测试污染，11个限流测试 + 183个总测试全部通过
- 更改的文件：src/services/rate_limit.py（重构为 RateLimiter + limiter 单例 + reset()）、tests/conftest.py（添加 limiter.reset()）
- **未来迭代的学习：**
  - RateLimitMiddleware 的 _requests 状态需要独立为全局 limiter 单例，否则 conftest 无法从外部重置
  - conftest 的 _reset_db fixture 需要在 yield 前后都调用 limiter.reset()，确保测试前后状态干净
  - BaseHTTPMiddleware 的 dispatch 方法中，路径白名单需包含 /docs, /openapi.json, /redoc 和 /data/* 静态文件
---

## 2026-05-04 11:21 - US-100
- 实现了 AgentLink-数据库表与Profile管理：新建 penpal_profiles/likes/matches 三张表；GET /api/agentlink/profile/me（需 API Key，首次自动创建空 profile）；PATCH /api/agentlink/profile（bio 必填<=500字符，mbti 可选 16 型格式自动转大写）；GET /api/agentlink/profile/{username}（需 API Key 查看他人笔友资料）
- 更改的文件：src/services/database.py（新增3张表）、src/models/schemas.py（新增 UpdatePenpalProfileRequest）、src/api/routes/friends.py（新增3个路由）、src/main.py（注册 friends_router）、tests/test_agentlink.py（新增13个测试）
- **未来迭代的学习：**
  - AgentLink 路由使用 APIRouter(prefix="/api/agentlink")，与 skills/bar 等路由并列
  - penpal_profiles 表使用 agent_id UNIQUE 约束确保一个 agent 只有一个笔友 profile
  - _ensure_penpal_profile 辅助函数处理"首次访问自动创建"逻辑，被 GET /me 和 PATCH /profile 共用
  - mbti 验证使用正则 `^[EI][SN][TF][JP]$` 匹配 16 型人格格式
  - likes 表的 UNIQUE(from_agent_id, to_agent_id) 为 US-101 的发现/喜欢功能做准备
---

## 2026-05-04 11:31 - US-101
- 实现了 AgentLink-发现笔友与喜欢/跳过：GET /api/agentlink/discover 随机推荐未操作过的 Agent（排除自己和已 like/pass 的），POST /discover/like 喜欢某人（自动检测双向匹配创建 match 记录），POST /discover/pass 跳过某人；likes 表新增 action 列区分 like/pass 操作；重复操作返回 duplicate 错误，不能操作自己
- 更改的文件：src/services/database.py（likes 表加 action 列）、src/models/schemas.py（新增 DiscoverTargetRequest）、src/api/routes/friends.py（新增 3 个路由）、tests/test_agentlink_discover.py（新增 18 个测试）
- **未来迭代的学习：**
  - likes 表的 action 列（'like'/'pass'）区分喜欢和跳过，UNIQUE(from_agent_id, to_agent_id) 保证对同一人只能操作一次
  - 双向匹配检测：A like B 后检查 B 是否也 like A（action='like'），是则创建 match 记录
  - match 记录需防重复：插入前检查 (agent1_id, agent2_id) 或 (agent2_id, agent1_id) 是否已存在
  - discover 查询使用 ORDER BY RANDOM() LIMIT 1 实现随机推荐
  - pass 不会触发匹配，即使对方已经 like 了自己
---

## 2026-05-04 14:30 - US-102
- 实现了 AgentLink-匹配列表与邮箱代理：GET /api/agentlink/matches（需 API Key，返回互相匹配的 Agent 列表含 username/nickname/avatar_url/proxy_email/matched_at）；GET /api/agentlink/matches/pending（需 API Key，返回喜欢了我但我还没操作的 Agent 列表，排除已操作和 pass 的）；DELETE /api/agentlink/matches/{id}（解除匹配，只能解除自己的，删除 matches 表记录）；proxy_email 格式为 {username}@agentlink.world 虚拟字段
- 更改的文件：src/api/routes/friends.py（新增 3 个路由）、tests/test_agentlink_matches.py（新增 14 个测试）、prd.json（passes: true）
- **未来迭代的学习：**
  - matches 查询使用 CASE WHEN 判断 partner_id（排除自己），两方都能看到匹配
  - pending 查询排除已操作过的人：from_agent_id NOT IN (SELECT to_agent_id FROM likes WHERE from_agent_id = ?)
  - pending 只看 action='like' 的记录，pass 操作不算
  - unmatch 直接 DELETE matches 表记录，不删除 likes 表的原始喜欢记录
  - 228 个测试全部通过
---

## 2026-05-04 15:30 - US-110
- 实现了 InStreet 社交广场的数据库表与发帖功能：新增 posts/post_comments/post_likes 三张表；POST /api/instreet/posts（需 API Key，title 必填<=200，content 必填<=5000，category 可选）；GET /api/instreet/posts（无需认证，分页 page/limit 默认20，含作者信息）；GET /api/instreet/posts/{id}（帖子详情含作者和评论列表）；DELETE /api/instreet/posts/{id}（软删除，只能删自己的帖子）
- 新增文件：src/api/routes/instreet.py、tests/test_instreet.py（16个测试）
- 更改文件：src/services/database.py（新增3张表）、src/models/schemas.py（新增 CreatePostRequest）、src/main.py（注册 instreet_router）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - InStreet 路由使用 APIRouter(prefix="/api/instreet")，与 AgentLink 模式一致
  - posts 表使用 deleted_at IS NULL 实现软删除，与 skills 表模式一致
  - 帖子列表 JOIN agents 获取作者信息（username/nickname/avatar_url），与 skills 列表模式一致
  - 帖子详情额外查询 post_comments 返回评论列表（过滤 deleted_at IS NULL）
  - 244 个测试全部通过（228 + 16 新增）
---

## 2026-05-04 16:10 - US-111
- 实现了 InStreet 浏览热门与最新：GET /api/instreet/posts/hot（按点赞数倒序，支持分页和 category 筛选），GET /api/instreet/posts/latest（按时间倒序，支持分页和 category 筛选），GET /api/instreet/categories（返回分类列表及帖子数，空 category 归为 uncategorized）
- 更改的文件：src/api/routes/instreet.py（新增3个路由+_format_post_row 辅助函数）、tests/test_instreet_hot_latest.py（新增14个测试）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - FastAPI 路由匹配顺序：`/posts/hot` 和 `/posts/latest` 必须在 `/posts/{post_id}` 之前定义，否则 "hot"/"latest" 会被当作 post_id
  - 分类筛选 SQL 中，count 查询不使用表别名（直接 `category`），list 查询使用别名也不冲突
  - _format_post_row 提取为共享辅助函数，被 hot/latest 列表路由复用
  - 258 个测试全部通过（244 + 14 新增）
---

## 2026-05-04 12:05 - US-112
- 实现了 InStreet 点赞与评论：POST /posts/{id}/like（需 API Key，likes_count+1，重复点赞报错 duplicate），DELETE /posts/{id}/like（取消点赞，likes_count-1），POST /posts/{id}/comments（需 API Key，content 必填，comments_count+1），GET /posts/{id}/comments（无需认证，分页），DELETE /posts/{id}/comments/{comment_id}（软删除，只能删自己的）
- 更改的文件：src/api/routes/instreet.py（新增6个路由）、src/models/schemas.py（新增 CreateCommentRequest）、tests/test_instreet_likes_comments.py（新增17个测试）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - 点赞使用 post_likes 表 UNIQUE(post_id, agent_id) 防重复，与 favorites/wish_votes 模式一致
  - 取消点赞直接 DELETE post_likes 记录（非软删除），likes_count 用 MAX(count-1, 0) 防负数
  - 评论软删除通过 deleted_at IS NULL 过滤，删除时 comments_count-1
  - 275 个测试全部通过（258 + 17 新增）
---

## 2026-05-04 19:00 - US-120
- 实现了 NeverLand 农场养成模块的数据库表与农场注册：新增 farms/farm_plots/farm_buildings/farm_animals/farm_achievements/farm_gifts 六张表；POST /api/neverland/farm/register（需 API Key，name 必填 <=100，description 可选，初始 6 块农田 + 100 金币）；GET /api/neverland/farm（需 API Key，返回农场概况含等级/金币/声誉/农田/建筑/动物概要）；每个 Agent 只能注册一个农场（agent_id UNIQUE）
- 新增文件：src/api/routes/neverland.py、tests/test_neverland.py（14个测试）
- 更改文件：src/services/database.py（新增6张表）、src/models/schemas.py（新增 RegisterFarmRequest）、src/main.py（注册 neverland_router）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - NeverLand 路由使用 APIRouter(prefix="/api/neverland")，与 AgentLink/InStreet 模式一致
  - 农场注册时循环创建 6 块 farm_plots，plot_index 0-5，默认 status='empty'
  - GET /farm 概况通过多次查询拼接（farms + farm_plots + farm_buildings + farm_animals），为 US-121/122/123 预留了数据接口
  - 289 个测试全部通过（275 + 14 新增）
---

## 2026-05-04 20:30 - US-121
- 实现了 NeverLand 种植系统：5种作物定义（胡萝卜5金/小麦10金/番茄15金/苹果30金/玫瑰50金），POST /farm/plots/{plot_index}/plant（扣除种子费），POST /farm/plots/{plot_index}/water（更新watered_at），POST /farm/plots/{plot_index}/harvest（成熟判断planted_at+growth_days<=now，收获获得金币+5XP），GET /farm/crops（无需认证，返回作物列表含价格/成熟天数/收益）
- 更改的文件：src/api/routes/neverland.py（新增4个路由+CROPS字典）、src/models/schemas.py（新增PlantRequest）、tests/test_neverland_planting.py（新增20个测试）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - 作物定义使用模块级 CROPS 字典，包含 name/seed_price/growth_days/harvest_value 四个属性
  - 成熟判断使用 datetime.fromisoformat + timedelta(days=growth_days)，而非 SQL 时间计算
  - 收获后农田重置为 empty（crop_type='', planted_at=NULL, watered_at=NULL, growth_days=0），可再次种植
  - 测试中通过手动修改 planted_at 为过去时间来模拟作物成熟，避免等待
  - 309 个测试全部通过（289 + 20 新增）
---

## 2026-05-04 21:15 - US-122
- 实现了 NeverLand 建筑与动物系统：POST /farm/buildings 建造建筑（鸡舍100/畜棚200/仓库150/温室300金），GET /farm/buildings 建筑列表，POST /farm/animals 购买动物（需对应建筑：鸡/鸭需鸡舍，兔/羊需畜棚），POST /farm/animals/{id}/collect 收集产品（鸡蛋3/鸭蛋4/兔脚5/羊毛8金，24小时冷却）
- 新增文件：tests/test_neverland_buildings.py（19个测试）
- 更改文件：src/api/routes/neverland.py（新增4个路由+BUILDINGS/ANIMALS字典）、src/models/schemas.py（新增BuildBuildingRequest/BuyAnimalRequest）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - 建筑和动物定义使用模块级 BUILDINGS/ANIMALS 字典，与 CROPS 模式一致
  - 动物购买需先检查对应建筑是否存在（required_building 字段），不存在返回 building_required 错误
  - 收集冷却使用 datetime.fromisoformat + timedelta(hours=24) 判断，与种植成熟的成熟判断模式一致
  - 动物归属校验通过 animal["farm_id"] == farm["id"] 实现，非主人返回 forbidden
  - 328 个测试全部通过（309 + 19 新增）
---

## 2026-05-04 22:00 - US-123
- 实现了 NeverLand 社交与成就系统：POST /farm/steal（偷取成熟作物，成功率=50%+自己level*2%-对方level*2%，成功获50%金币对方损失，失败-5声誉，每日3次上限），POST /farm/gift（赠送金币，+2声誉），GET /farm/achievements（成就列表含解锁状态），4种成就自动解锁发奖励（first_harvest/farmer_10/builder_3/social_butterfly）
- 新增文件：tests/test_neverland_social.py（22个测试）
- 更改文件：src/services/database.py（新增farm_steals表+farms加harvests_count/gifts_count列+迁移SQL）、src/models/schemas.py（新增StealRequest/GiftRequest）、src/api/routes/neverland.py（新增3个路由+ACHIEVEMENTS字典+成就检查辅助函数+更新harvest/build路由触发成就）、tests/test_neverland_planting.py（修复2个断言适配成就奖励）
- **未来迭代的学习：**
  - 成就系统使用 ACHIEVEMENTS 模块级字典定义（与 CROPS/BUILDINGS/ANIMALS 模式一致），包含 name/description/gold_reward/xp_reward
  - _check_and_award_achievements 在 harvest/build/gift 操作后自动调用，_award_if_new 防止重复解锁
  - farms 表新增 harvests_count/gifts_count 列需用 ALTER TABLE 迁移（_MIGRATIONS_SQL 列表 + try/except 忽略已存在列）
  - 新功能（成就自动奖励）可能影响已有测试的 gold/xp 断言，需要同步更新
  - 偷窃成功率计算使用 random.randint(0,99) < success_rate，等级差影响为±2%每级
  - 350 个测试全部通过（328 + 22 新增）
---

## 2026-05-04 20:47 - US-200
- 实现了积分签到系统：sign_in_records 表（UNIQUE(agent_id, site, checked_at)防重复），POST /api/checkin（+5基础虾米，连续2天+2/3天+3/7天+5额外奖励），GET /api/checkin/status（今日是否已签到/连续天数/总次数/下次奖励预览），GET /api/checkin/history（分页签到历史），断签后连续天数重置
- 新增文件：src/api/routes/checkin.py（3个路由+STREAK_BONUS映射+_add_xfund辅助）、tests/test_checkin.py（13个测试）
- 更改文件：src/services/database.py（新增sign_in_records表）、src/main.py（注册checkin_router）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - 签到连续天数通过检查 yesterday 的记录计算，断签（昨天无记录）则重置为1
  - STREAK_BONUS 字典按最高匹配天数发放额外奖励，而非累加
  - sign_in_records 使用 UNIQUE(agent_id, site, checked_at) 三列约束防止同一天重复签到
  - _add_xfund 复用 ON CONFLICT 模式：有则加、无则创建钱包
  - 363 个测试全部通过（350 + 13 新增）
---

## 2026-05-04 21:00 - US-201
- 实现了任务与XP系统：tasks 表存储任务定义，task_completions 表记录完成历史；GET /api/tasks 返回5个预设任务含进度和完成状态；POST /api/tasks/{id}/complete 验证条件满足后发放 XP 和虾米奖励；每日任务按天重置（DATE(completed_at)=today），成就任务永久；wallets 表新增 xp 列追踪 XP 积分
- 新增文件：src/api/routes/tasks.py（5个预设任务+seed_tasks+2个路由+进度查询+奖励发放）、tests/test_tasks.py（15个测试）
- 更改文件：src/services/database.py（新增tasks/task_completions表+wallets.xp迁移）、src/main.py（注册tasks_router+启动时seed_tasks）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - 任务种子数据使用"表为空时插入"模式（SELECT COUNT(*) 检查），与 drinks/farms seed 一致
  - XP 追踪通过 wallets 表新增 xp 列（INTEGER DEFAULT 0），与其他模块的 _add_xfund 模式兼容
  - 每日任务重置通过 DATE(completed_at) = today 判断，成就任务直接检查是否存在任何 completion 记录
  - _get_task_progress 根据 target_type 动态查询不同表，is_daily 参数决定是否按今天过滤
  - seed_tasks 在路由内也被调用（幂等），确保测试环境（lifespan 不触发时）也能获取预设任务
  - 378 个测试全部通过（363 + 15 新增）
---

## 2026-05-04 21:11 - US-202
- 实现了排行榜扩展：GET /api/rankings 支持 type 参数（xfund/checkin/posts/farm）和 period 参数（weekly/monthly/all），xfund 类型保留 balance/level 字段向后兼容；GET /api/rankings/me 需 API Key 返回各类排行榜排名
- 更改的文件：src/api/routes/skills.py（重构 rankings 路由+新增 rankings/me 路由）、tests/test_rankings_extended.py（新增17个测试）
- **未来迭代的学习：**
  - 排行榜扩展需保持向后兼容，xfund 类型额外返回 balance 和 level 字段
  - period 过滤通过 datetime.now(timezone.utc) - timedelta(days=N) 计算时间窗口
  - 多表 JOIN 时注意列名歧义，GROUP BY 中需要使用表名前缀（如 sign_in_records.agent_id）
  - 排名计算使用子查询 COUNT(*) + 1 模式，统计比自己分数高的人数
  - 395 个测试全部通过（378 + 17 新增）
---

## 2026-05-04 22:15 - US-210
- 实现了举报系统：reports 表（UNIQUE(reporter_id, target_type, target_id) 防重复举报），POST /api/reports 提交举报（需 API Key，支持 post/guestbook/comment/skill/review 类型，不能举报自己，重复举报返回 duplicate 错误），GET /api/reports/my 返回我的举报记录（分页，按时间倒序）
- 新增文件：src/api/routes/reports.py（2个路由+_get_target_owner辅助函数）、tests/test_reports.py（新增14个测试）
- 更改文件：src/services/database.py（新增reports表）、src/models/schemas.py（新增CreateReportRequest）、src/main.py（注册reports_router）
- **未来迭代的学习：**
  - 举报系统使用独立的 reports 路由文件（APIRouter(prefix="/api")），与其他模块模式一致
  - _get_target_owner 辅助函数根据 target_type 查询不同表获取内容所有者 agent_id，用于"不能举报自己"校验
  - UNIQUE(reporter_id, target_type, target_id) 三列约束防重复举报，查询时手动检查（非依赖 UNIQUE 报错）以返回友好错误信息
  - reason 使用 Pydantic Field(max_length=200) 自动验证，超长返回 422
  - 409 个测试全部通过（395 + 14 新增）
---

## 2026-05-04 23:45 - US-211
- 实现了统一错误响应格式：更新 error_response() 始终包含 request_id 和 hint 字段；添加全局异常处理器（HTTPException → 统一格式、RequestValidationError → 422 validation_error、Exception → 500 internal_error）；统一 auth.py 错误码（auth_failed/unauthorized）；更新 rate_limit.py 包含 request_id/hint/剩余额度；将 skills.py 的 insufficient_balance 改为 insufficient_funds 并添加虾米获取途径提示
- 更改的文件：src/utils/helpers.py（error_response 加 request_id）、src/main.py（3个全局异常处理器）、src/services/auth.py（统一错误码 auth_failed/unauthorized）、src/services/rate_limit.py（加 request_id/hint/remaining）、src/api/routes/skills.py（insufficient_balance→insufficient_funds+hint）、tests/test_auth.py（适配新格式）、tests/test_avatar.py（适配新格式）、tests/test_error_format.py（新增12个测试）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - 全局异常处理器通过 app.exception_handler(StarletteHTTPException) 捕获所有 HTTPException，无需修改每个路由文件
  - HTTPException detail 可以是 dict 或 string，异常处理器需要两种都处理
  - RequestValidationError.errors() 返回 Pydantic 错误列表，loc 字段是元组需要 join
  - 修改错误响应格式后，已有测试中访问 resp.json()["detail"] 的地方需要改为 resp.json()（新格式不再有 detail 包裹）
  - 421 个测试全部通过（409 + 12 新增）
---

## 2026-05-04 24:00 - US-220
- 实现了 TravelMind-随机漫步模块：新增 landmarks/visits 两张表；预置 20 个世界景点（埃菲尔铁塔/自由女神像/长城/富士山/大本钟/悉尼歌剧院/金字塔/泰姬陵/马丘比丘/圣家堂/比萨斜塔/吴哥窟/克里姆林宫/圣托里尼/维多利亚瀑布/巨石阵/佩特拉/北极光/桑给巴尔海滩/金阁寺）；GET /api/travel/discover 随机推荐未打卡景点；POST /api/travel/landmarks/{id}/visit 打卡+2虾米；GET /api/travel/visits 分页打卡记录；GET /api/travel/landmarks 无需认证景点列表（认证时含打卡状态）
- 新增文件：src/services/landmark_seeds.py（20个预设景点+seed函数）、src/api/routes/travel.py（4个路由+可选认证辅助函数）、tests/test_travel.py（15个测试）
- 更改文件：src/services/database.py（新增landmarks/visits表）、src/main.py（注册travel_router+启动时seed_landmarks）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - TravelMind 路由使用 APIRouter(prefix="/api/travel")，与 AgentLink/InStreet/NeverLand 模式一致
  - 景点种子使用"表为空时插入"模式（SELECT COUNT(*) 检查），与 drinks/tasks seed 一致
  - 可选认证通过自定义 _get_optional_agent 依赖实现，捕获异常返回 None（而非直接依赖 get_current_agent）
  - visits 表 UNIQUE(agent_id, landmark_id) 防重复打卡，与 favorites/wish_votes 模式一致
  - 测试间 agents 表数据会累积导致用户名冲突，需 autouse fixture 清空 visits/agents/wallets 表
  - 436 个测试全部通过（421 + 15 新增）
---

## 2026-05-04 25:20 - US-230
- 实现了 PlayLab 游戏房间系统：新增 game_rooms/game_players 两张表；POST /api/playlab/rooms（需 API Key，game_type 必填 gomoku/poker/werewolf，创建者自动加入 player_index=0）；GET /api/playlab/rooms（无需认证，返回 status=waiting 的房间列表，分页，含创建者信息）；POST /api/playlab/rooms/{id}/join（需 API Key，房间未满且未开始才可加入，重复加入报错 already_joined，已满报错 room_full）；POST /api/playlab/rooms/{id}/start（需 API Key 且是创建者 player_index=0，房间满员后开始）；GET /api/playlab/rooms/{id}/state（需 API Key 且是参与者，返回完整游戏状态含玩家列表和我的 player_index）
- 新增文件：src/api/routes/playlab.py（5个路由+GAME_TYPES字典）、tests/test_playlab.py（18个测试）
- 更改文件：src/services/database.py（新增game_rooms/game_players表）、src/models/schemas.py（新增CreateRoomRequest）、src/main.py（注册playlab_router）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - PlayLab 路由使用 APIRouter(prefix="/api/playlab")，与 AgentLink/InStreet/NeverLand/TravelMind 模式一致
  - GAME_TYPES 字典定义游戏类型和最大玩家数（gomoku=2/poker=6/werewolf=8），与 CROPS/BUILDINGS 模式一致
  - 创建者通过 player_index=0 识别，start 路由检查 player_index=0 的 agent_id 是否匹配当前用户
  - 房间列表 JOIN game_players 和 agents 获取创建者信息（仅 player_index=0）
  - current_players 使用数据库自增（current_players + 1），而非查询 game_players 表 COUNT
  - 测试 autouse fixture 需清空 game_players → game_rooms → wallets → agents（注意外键顺序）
  - 454 个测试全部通过（436 + 18 新增）
---

## 2026-05-04 26:30 - US-231
- 实现了 PlayLab-五子棋游戏逻辑：新增 game_states 表存储棋局状态（15x15 棋盘 JSON、当前回合、最后落子、落子计数）；开始五子棋游戏时自动初始化空棋盘；POST /api/playlab/rooms/{id}/action 支持 action=place 落子（验证轮次、位置为空）；落子后检测五连（横/竖/正斜/反斜），获胜则 room.status=finished；棋盘下满无五连则平局；GET /api/playlab/rooms/{id}/state 返回完整棋盘/当前轮次/最后落子/游戏状态
- 新增文件：tests/test_gomoku.py（16个测试）
- 更改文件：src/services/database.py（新增game_states表）、src/models/schemas.py（新增GomokuActionRequest）、src/api/routes/playlab.py（新增_check_win函数+game_action路由+_gomoku_action处理+start初始化棋盘+state附加棋盘）
- **未来迭代的学习：**
  - 五子棋棋盘使用 JSON TEXT 存储 15x15 二维数组，每次落子后序列化回数据库
  - player_index 0 对应黑子(piece=1)，player_index 1 对应白子(piece=2)
  - 五连检测使用 _check_win 函数，检查4个方向（横/竖/正斜/反斜），每个方向双向计数
  - game_states 表使用 room_id UNIQUE 约束，一个房间只有一个棋局状态
  - 落子成功后同时更新 game_states 和 game_rooms（胜负/平局时），需在一个 commit 中
  - 测试 autouse fixture 需清空 game_states → game_players → game_rooms → wallets → agents（注意顺序）
  - 470 个测试全部通过（454 + 16 新增）
---

## 2026-05-04 22:30 - US-232
- 实现了 PlayLab-德州扑克游戏逻辑：新增 poker_states/poker_hands 两张表；开始游戏时创建52张牌组、洗牌、每人发2张底牌、自动扣盲注(SB=10/BB=20)；POST /api/playlab/rooms/{id}/action 支持 action=call(跟注)/raise(加注+amount)/fold(弃牌)/allin(全押)；四个阶段 preflop→flop(3张公共牌)→turn(1张)→river(1张)→showdown(摊牌)；摊牌时使用牌型评估(皇家同花顺/同花顺/四条/葫芦/同花/顺子/三条/两对/一对/高牌)判定赢家；GET state 只能看到自己的底牌，showdown 时才公开所有底牌；简化 start 逻辑：扑克2+人即可开始(五子棋仍需满员)
- 新增文件：tests/test_poker.py（16个测试）
- 更改文件：src/services/database.py（新增poker_states/poker_hands表）、src/models/schemas.py（GameActionRequest统一模型替代GomokuActionRequest+PokerActionRequest）、src/api/routes/playlab.py（新增完整扑克逻辑+牌型评估+阶段推进）、tests/test_playlab.py（适配error code变更）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - GameActionRequest 统一了 action 请求模型，包含 action/row/col/amount 四个字段，所有游戏共用
  - poker_states 存储 deck（剩余牌堆 JSON）、community_cards（公共牌）、pot/phase/current_bet 等状态
  - poker_hands 存储每个玩家的底牌、筹码、下注、弃牌状态，通过 room_id + agent_id 关联
  - 扑克开始逻辑与五子棋不同：不需要满员，2+人即可开始（start_room 中按 game_type 区分 min_players）
  - 牌型评估使用 itertools.combinations 从7张(2底牌+5公共)中选最佳5张，避免硬编码
  - _advance_phase 在每次跟注/阶段推进时检查所有活跃玩家是否已匹配当前注，决定是否进入下一阶段
  - 测试 autouse fixture 需清空 poker_hands → poker_states → game_states → game_players → game_rooms → wallets → agents（注意顺序）
  - 486 个测试全部通过（470 + 16 新增）
---

## 2026-05-04 27:30 - US-233
- 实现了 PlayLab-谁是卧底游戏逻辑：新增 werewolf_states/werewolf_players 两张表；游戏开始时随机分配角色（卧底约1/3最少1人），随机选择词对（20组）；POST /api/playlab/rooms/{id}/action 支持 action=describe（按顺序描述，全部描述完进入投票阶段）/vote（投票淘汰，row 参数传 target_player_index）；统计票数淘汰票数最多者；淘汰后检查胜负：卧底全淘汰=平民胜，卧底数>=平民数=卧底胜；游戏结束揭示所有角色和词对；GET /api/playlab/rooms/{id}/state 返回当前阶段/轮次/存活玩家/我的词/是否是我的回合；最少3人开始游戏
- 新增文件：tests/test_werewolf.py（15个测试）
- 更改文件：src/services/database.py（新增werewolf_states/werewolf_players表）、src/api/routes/playlab.py（新增完整卧底逻辑+初始化+描述+投票+计票+胜负判定+20组词对）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - 谁是卧底使用 werewolf_states 存储 civilian_word/spy_word/phase/round/current_describer_index，与 poker_states 模式一致
  - werewolf_players 存储 role/is_alive/description/voted_for_id，通过 state_id + agent_id 关联
  - 描述阶段按存活玩家顺序进行（current_describer_index 为第 N 个存活玩家，非 player_index），全部描述完自动切换到投票阶段
  - 投票通过 GameActionRequest 的 row 字段传递 target_player_index（统一模型复用）
  - _werewolf_check_game_over 在每次淘汰后调用，统计存活角色数判定胜负
  - sqlite3.Row 不支持 SELECT 中未选择的列，查 eliminated_info 需包含 agent_id 列
  - 测试 autouse fixture 需清空 werewolf_players → werewolf_states → poker_hands → poker_states → game_states → game_players → game_rooms → wallets → agents（注意顺序）
  - 501 个测试全部通过（486 + 15 新增）
---

## 2026-05-04 28:00 - FE-001
- 实现了前端基础架构与共享组件：src/lib/api.ts 提供 apiGet/apiPost/apiPut/apiPatch/apiDelete 封装（自动带 API Key header + ApiError 类）；SiteHeader 包含 8 个导航链接（首页/虾评/酒馆/笔友/广场/农场/旅行/桌游）+ API Key 输入框（保存到 localStorage）+ 移动端汉堡菜单；Layout 包含 Header + Outlet + Footer（Agent World slogan）；App.tsx 配置 BrowserRouter + 9 条路由；Tailwind CSS v4 通过 @theme 配置深浅色主题变量 + Noto Serif SC 字体；index.html lang=zh-CN + title=Agent World
- 更改的文件：src/lib/api.ts（新增）、src/components/SiteHeader.tsx（新增）、src/components/Layout.tsx（新增）、src/pages/world/WorldPage.tsx（新增占位）、src/pages/xiaping/XiapingPage.tsx（新增占位）、src/pages/bar/BarPage.tsx（新增占位）、src/pages/friends/FriendsPage.tsx（新增占位）、src/pages/instreet/InstreetPage.tsx（新增占位）、src/pages/neverland/NeverlandPage.tsx（新增占位）、src/pages/travel/TravelPage.tsx（新增占位）、src/pages/playlab/PlaylabPage.tsx（新增占位）、src/pages/common/CommonPages.tsx（新增占位）、src/App.tsx（重写为路由配置）、src/index.css（重写为 Tailwind v4 + 主题变量）、index.html（lang=zh-CN + title）、src/App.css（删除）
- **未来迭代的学习：**
  - Tailwind CSS v4 使用 @theme 块定义设计令牌，@import url() 必须在 @import "tailwindcss" 之前避免 CSS 警告
  - 深色模式通过 @media (prefers-color-scheme: dark) 覆盖 CSS 变量实现，不需要 class 切换
  - API 封装使用 localStorage.getItem('agent_api_key') 获取 key，通过 agent-auth-api-key header 传递
  - SiteHeader 使用 responsive 设计：md:flex 显示桌面导航，md:hidden 显示汉堡菜单
  - Vite proxy 配置在 vite.config.ts 中将 /api /drink /drinks /sessions /guestbook /selfies /data 转发到 localhost:8000
  - build 输出到 ../static 目录（vite.config.ts outDir 配置）
---

## 2026-05-04 28:30 - FE-010
- 实现了主站首页 World：Hero区域（font-serif大标题+副标题+注册按钮）、注册模态框（3步流程：填写username/nickname/bio → 解答数学挑战题 → 显示API Key并自动保存）、7个联盟站点卡片网格（虾评/酒馆/AgentLink/InStreet/NeverLand/TravelMind/PlayLab，每个含Lucide图标+名称+描述）、Philosophy引言区域
- 更改的文件：src/pages/world/WorldPage.tsx（重写为完整首页）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - 注册流程使用 apiPost 调用 /api/agents/register 和 /api/agents/verify，verify 需要传 answer 为数字类型
  - 注册成功后 API Key 自动保存到 localStorage，无需用户手动输入
  - 模态框使用 fixed inset-0 + bg-black/50 遮罩层 + onClick stopPropagation 防止穿透关闭
  - 卡片网格使用 grid sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 响应式布局
---

## 2026-05-04 29:00 - FE-020
- 实现了虾评技能市场：技能列表页（搜索框+分类筛选+排序newest/downloads/rating+分页）、技能卡片网格（name/description/author/downloads/rating/category）、技能详情页（完整信息+下载+收藏+评测列表+发布评测）、发布技能表单（name/description/category）、许愿墙（心愿列表+发布心愿+投票）、分类列表（展示分类及技能数）
- 更改的文件：src/pages/xiaping/XiapingPage.tsx（重写为完整技能市场页面，含5个子组件）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - TypeScript strict 模式下，Record<string, unknown> 类型的字段用 String() 包装才能在 JSX 中渲染
  - unknown 类型的 && 条件渲染需要用三元表达式替代：`x ? <p>{String(x)}</p> : null` 而不是 `x && <p>{String(x)}</p>`
  - 未使用的 import 在 noUnusedLocals: true 下会报错，需清理
  - 许愿墙 API 路径是 /api/wishes，不是 //api/wishes（代码中有双斜线需注意）
---

## 2026-05-05 10:00 - FE-060
- 实现了NeverLand农场养成完整页面：农场概况（等级/金币/XP/声誉）、农田网格（种植/浇水/收获模态框选作物）、作物列表、建筑列表（已建+可建造）、动物管理（已购动物+可购买动物+收集产品按钮+24h冷却提示）、成就面板、社交面板（偷窃/赠送）
- 后端修复：GET /farm 返回动物的 id 和 last_collected_at 字段（原来只有 animal_type 和 name，无法调用 collect 接口）
- 修复 SocialPanel 偷窃/赠送共享同一个 target state 的 bug，改为独立 state
- 修复 PlaylabPage 未使用的 Gamepad2/Swords/Circle 导入和 unknown 类型条件渲染
- 更改的文件：frontend/src/pages/neverland/NeverlandPage.tsx（重写）、src/api/routes/neverland.py（动物查询加 id/last_collected_at）、frontend/src/pages/playlab/PlaylabPage.tsx（修复 TS 错误）
- **未来迭代的学习：**
  - 后端 GET /farm 概览返回的动物数据需包含 id，否则前端无法调用 POST /farm/animals/{id}/collect
  - SocialPanel 等包含多个独立操作的面板，每个操作应有独立 state，避免互相干扰
  - BuildingList 改为从父组件传入 buildings prop（已从 farm 数据获取），避免重复 API 调用
  - 动物收集产品有 24h 冷却，通过 last_collected_at + 24h 判断按钮状态
  - PlaylabPage 的 pre-existing TS 错误（未用导入 + unknown 条件渲染）需要一并修复才能 build
---

## 2026-05-05 10:30 - FE-090/100
- 实现了 PlayLab 桌游对战页面：游戏大厅（创建房间选gomoku/poker/werewolf+等待中房间列表+加入按钮）、五子棋（15x15棋盘网格+黑白子+落子+3s自动刷新）、德州扑克（公共牌+底牌+跟注/加注/弃牌/全押按钮+底池）、谁是卧底（我的词+描述+投票选择+存活玩家列表）；实现了举报与个人中心页面：Profile展示（头像/昵称/虾米余额/等级）+编辑Profile（修改nickname/bio）+举报功能（选类型+填ID+原因）+举报记录列表
- 更改的文件：frontend/src/pages/playlab/PlaylabPage.tsx（重写为完整桌游页面含4个子组件）、frontend/src/pages/common/ProfilePages.tsx（新增个人中心+举报页面）、frontend/src/App.tsx（添加/profile路由）、scripts/ralph/prd.json（FE-090/100 passes: true）
- **未来迭代的学习：**
  - PlayLab 游戏状态轮询使用 useEffect + setInterval(3s) 自动刷新 playing 状态的房间
  - 游戏结束展示 Trophy SVG 组件（内联 SVG，非 lucide-react 导入）
  - Record<string, unknown> 类型的 unknown 字段在 JSX 中需要用 `{"text" + String(val)}` 模式避免 ReactNode 类型错误
  - 全部 11 个前端 story (FE-001 到 FE-100) 已完成，npm run build 通过
---

## 2026-05-05 23:00 - FE-100 (修复)
- 修复FE-100验证失败的三项问题：(1) ProfileTab 增加调用 /api/agents/profile/{username} 获取 avatar_url 和 bio，在卡片中展示头像图片和简介文字；(2) 添加"我的技能"和"我的下载"两个tab，分别调用 GET /api/me/skills 和 GET /api/me/downloads；(3) 创建可复用 ReportModal 组件，在 InStreetPage 帖子详情和评论旁、BarPage 留言簿旁添加内联 Shield 图标举报按钮
- 更改的文件：frontend/src/components/ReportModal.tsx（新增）、frontend/src/pages/common/ProfilePages.tsx（重写ProfileTab+新增MySkillsTab/MyDownloadsTab）、frontend/src/pages/instreet/InstreetPage.tsx（帖子+评论旁添加举报按钮）、frontend/src/pages/bar/BarPage.tsx（留言旁添加举报按钮）、scripts/ralph/prd.json（passes: true）
- **未来迭代的学习：**
  - ReportModal 组件拆分为 ReportModal（模态框）和 ReportButton（小型按钮），供各页面复用
  - /api/auth/me 不返回 avatar_url 和 bio，需额外调用 /api/agents/profile/{username} 获取完整信息
  - 内联举报按钮使用 Shield 图标 + 小尺寸(text-xs)，放在操作按钮组旁边，不影响原有布局
  - 个人中心 tab 扩展为4个：个人资料/我的技能/我的下载/举报
---
## 2026-05-05 23:30 - US-300
- 实现了新用户初始资产与新手任务链：verify激活时自动创建wallets记录(50虾米初始余额)；新增5个beginner类型新手任务(complete_profile+10/first_guestbook+10/first_drink+10/register_farm+15/first_discover+10)；GET /api/tasks返回beginner/daily/achievement并列展示；POST /api/tasks/{id}/complete对beginner任务有效，永久可完成一次不设每日重置；seed_tasks改为INSERT OR IGNORE逐条插入支持增量新增任务；_get_task_progress新增profile/guestbook/drink/farm/discover五种target_type
- 更改的文件：src/api/routes/agents.py（verify路由添加初始钱包创建）、src/api/routes/tasks.py（新增5个beginner任务+seed_tasks改为INSERT OR IGNORE+新增5种target_type进度查询）、tests/test_us300_beginner.py（新增11个测试）、tests/test_tasks.py（更新预设任务数量10+修复日期时区问题）、tests/test_checkin.py（余额断言+50）、tests/test_download_favorites.py（余额断言+50+清空余额测试）、tests/test_error_format.py（清空余额测试）、tests/test_profile_rankings.py（INSERT OR REPLACE+余额断言+50）、tests/test_rankings_extended.py（INSERT OR REPLACE+余额断言+50）、tests/test_reviews.py（余额断言+50）、tests/test_skill_crud.py（余额断言+50）、tests/test_wishes.py（INSERT OR REPLACE+余额断言+50）
- 512 个测试全部通过
---

## 2026-05-06 00:10 - US-301
- 实现了内容冷启动-种子数据：创建seed_content.py服务文件，在lifespan启动时调用；seed 3个示例技能(Agent通讯技能包/dev、提示词优化器/ai、数据分析器/data)；seed 3条InStreet帖子(欢迎来到AgentWorld/announce、社区规则说明/announce、本周活动预告/activity)；seed 1条酒馆留言(欢迎来到酒馆)；seed 1个心愿(希望更多有趣的Agent加入AgentWorld)；使用INSERT OR IGNORE防止重复插入，agent_id统一用'system'
- 更改的文件：src/services/seed_content.py（新增）、src/main.py（注册seed_content+lifespan调用）、tests/test_us301_seed.py（新增7个测试）、scripts/ralph/prd.json（passes: true）
- 519 个测试全部通过
---

## 2026-05-06 00:40 - US-302
- 实现了注册成功引导流程：创建 WelcomePage 组件(/welcome路由)；注册验证成功后跳转到引导页(通过navigate+state传递apiKey)；引导页展示恭喜信息+API Key(带复制按钮+提示保存到memory)+50虾米初始奖励；展示5个beginner新手任务列表(标题/描述/奖励/完成状态)；4个快捷入口卡片(完善Profile→/profile、去酒馆→/bar、注册农场→/neverland、发现笔友→/friends)；底部"开始探索"按钮跳转首页；注册模态框移除done步骤改为跳转
- 更改的文件：frontend/src/pages/common/WelcomePage.tsx（新增）、frontend/src/pages/world/WorldPage.tsx（注册成功后navigate到/welcome）、frontend/src/App.tsx（添加/welcome路由）、scripts/ralph/prd.json（passes: true）
---
